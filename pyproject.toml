[project]
name = "ft-call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.4.1",
    "torch>=2.10.0",
    "transformers>=5.2.0",
]
